# Industrial Gauge Reading

本项目识别工业仪表，并将通用的仪表类型知识与单张图片中的观测值分开管理。

## Language

**仪表类型（Instrument Type）**:
具有相同用途、显示区域和读数语义的一类物理仪表；具体型号可以归属于同一仪表类型。
_Avoid_: 图片类别、表盘模板

**类型知识（Type Knowledge）**:
适用于某一仪表类型的专业事实，包括显示区域含义、单位、量程、解释规则和证据来源。
_Avoid_: 通用 metadata、图片读数

**类型知识包（Type Knowledge Bundle）**:
以仪表类型为目录边界，将 `metadata.json` 与已核验的厂家说明书放在一起；查询程序负责跨目录汇总。
_Avoid_: 单一大 JSON、散落的说明书

**读数通道（Readout Channel）**:
仪表上需要独立识别和解释的一个显示区域，例如模拟指针、机械动作计数窗或数字显示屏。
_Avoid_: 字段、OCR 文本块

**观测值（Observation）**:
从一张具体图片的某个读数通道获得的值，并保留图片、置信度和人工确认状态等上下文。
_Avoid_: 类型知识、默认值

**原始视觉读数（Raw Visual Reading）**:
通用视觉与几何算法直接计算出的连续值；即使后续被类型知识归整，也必须保留作为审计依据。
_Avoid_: 最终读数、人工确认值

**业务读数（Business Reading）**:
原始视觉读数经过已匹配仪表类型的单位、允许值和解释规则处理后，对外使用的结果；没有唯一类型匹配时不得擅自归整。
_Avoid_: 原始模型输出、无依据修正值

**最终复核值（Final Reviewed Value）**:
经过人工确认后用于本次现场记录的值。它可以覆盖自动结果的业务使用优先级，但不能删除、改写或伪装成自动识别结果。
_Avoid_: 模型真值、训练标签回灌

**解释规则（Interpretation Rule）**:
说明读数通道应如何转换为业务含义，以及哪些看似相邻的显示值不能混为一谈的专业规则。

**读数归整（Reading Normalization）**:
仅对 metadata 明确声明的离散读数通道，将足够接近允许值的原始视觉读数映射为业务读数；超过允许距离时拒绝输出，而不是强行取整。

**证据来源（Evidence Source）**:
支持某条类型知识的厂家资料、产品手册、表盘文字或人工确认记录。

**持续泄漏电流（Continuous Leakage Current）**:
运行电压下持续通过避雷器的电流，由避雷器监测器的模拟指针通道显示。

**避雷器动作计数（Arrester Operation Count）**:
避雷器因雷击或过电压发生放电动作的累计次数，由机械计数窗显示；它不是电流值。

**机构操作计数（Mechanism Operation Count）**:
电动机构完成分接操作的累计次数，由机械计数窗显示；它不是当前分接位置。

**单圈计数显示值（Single-revolution Counter Display）**:
单指针在 `0–9` 圆形数字盘上指示的当前可见数字；没有进位、复位或检修记录时，不等同于设备全寿命累计次数。
